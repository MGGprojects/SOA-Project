package com.example.authservice.model;

/**
 * Defines the supported roles in the platform.
 * These roles will later be used to protect endpoints and authorize actions.
 */
public enum UserRole {
    CUSTOMER,
    BUSINESS
}
